Q: What is a javascript REPL? Can you name two ways to open one?

A: {A javascript REPL is an interactive programmin g environment that takes user inputs (Read), executes them (eval) and returns the result to the user(print)before waiting for the next inpout (loop). Types include ,The browser console and the node.js terminal}


Remember to commit and push when you're finished!
